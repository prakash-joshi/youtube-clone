import SideBar from "../components/SideBar";
import VideoContainer from "../components/VideoContainer";

const Home = () => {
  return (
    <div className="flex">
      <SideBar />
      <VideoContainer />
    </div>
  );
};

export default Home;
